vspd = random_range(3, 6); // random fall speed



// Set a random direction from 0 to 360 degrees
direction = random(360);

// Set a random speed between 2 and 6 (you can adjust this range)
speed = random_range(2, 6);

alarm[0] = room_speed * 2; // change every 2 seconds
